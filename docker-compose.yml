services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: gotrip
      POSTGRES_USER: gotrip_user
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data